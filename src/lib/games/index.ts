export { calculateMistakeScore, calculateTimeScore, calculateTotalScore } from "./scoring";
export type {
    Difficulty,
    GameResult,
    BaseLevelConfig,
    ScoringConfig,
    ScoringThreshold,
} from "./types";
